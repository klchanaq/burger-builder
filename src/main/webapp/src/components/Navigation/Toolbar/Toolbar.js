import React from "react";
import "./Toolbar.css";

const toolbar = function(props) {
  return (
    <header className="Toolbar">
      <div>Menu</div>
      <div>Logo</div>
      <nav>...</nav>
    </header>
  );
};

export default toolbar;
